import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {},
  chipLayout: {
    flexDirection: "row",
    flex: 1,
    flexWrap: "wrap",
    width: "100%",
    backgroundColor: "green",
  },
});
